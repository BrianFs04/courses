// JavaScript Object Notation

const { name, edad, email } = {
	name: 'Brian',
	edad: 20,
	email: 'mail@mail.com',
};

//const { name, edad, email } = Usuario;

document.write(`<p> ${name} </p>`);
document.write(`<p> ${edad} </p>`);
document.write(`<p> ${email} </p>`);
