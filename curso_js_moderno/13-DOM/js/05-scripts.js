// Returns every element that matches with the selector
const cards = document.querySelectorAll(".card");
console.log(card);

// If an element does not exist
const notExist = document.querySelectorAll("notExist");
console.log(notExist); // Empty node list
