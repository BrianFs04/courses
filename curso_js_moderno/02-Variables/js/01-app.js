// Initialize a variable with a value
var product = "24 inches monitor";
console.log(product);

// Variables can be reasigned
product = "19 inches monitor";
console.log(product);

//JS is a dynamic language, it does not require type of data
product = 24;
console.log(product);

// It is possible to initialize a variable without a value and asign one to it after
var available;
available = true;

available = false;

// Initialize multiple variables
var category = "Computers",
    brand = "Famous brand",
    score = 10;

/* 
Variables can't start with a number
var 99example;
var 5ex;
We use camelCase for variables and PascalCase for classes usually in JS 
var camelCase
class PascalCase
*/
