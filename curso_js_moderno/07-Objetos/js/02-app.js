const product = {
    name: "24 inches monitor",
    price: 300,
    available: true,
};

console.log(product);

// Ways to access to a key of an object
console.log(product.name);
console.log(product["name"]);
