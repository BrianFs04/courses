const name = "24 inches monitor";
const price = 300;
const available = true;

// Objects have everything in just one variable

//Object literal
const product = {
    name: "24 inches monitor",
    price: 300,
    available: true,
};

console.log(product);
