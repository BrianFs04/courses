const product = "24 inches monitor";

// To know the quantity of letter in a string or the length
console.log(product.length);

//console.log(product.indexOf("inches"));

console.log(product.includes("monitor"));
